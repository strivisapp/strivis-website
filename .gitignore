# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files (public repo): never commit real values, only the example
.env
.env.*
!.env.example

# Private keys and OAuth client secrets (Apple .p8, Google client_secret JSON,
# PEM/P12) must never be committed, even by accident with `git add .`.
*.p8
*.p12
*.pem
*.key
client_secret*.json
*service-account*.json
