// The App Store listing the site's download buttons point to. Strivis is a
// native-only app, so this is the only place a visitor can get it. Set it to
// the real listing (https://apps.apple.com/app/id<Apple ID>) once the app is
// approved — until then the buttons show "Bald im App Store" without a link.
export const APP_STORE_URL = null;
